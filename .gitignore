# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Dev EOD-import target DB (STORY-031) and local SQLite market-data builds
dev-market.db
*.db
# SQLite WAL/SHM sidecar files (regenerated alongside the DB)
*.db-shm
*.db-wal
# Pointer to the last-imported dev dataset (STORY-031) — local state, regenerable
.dev-active-db

# Local secrets. ANTHROPIC_API_KEY for the portfolio screenshot reader lives here
# (hardening stage 3); the repository is public, so this rule exists before the file does.
.env
.env.*

# Claude Code personal settings
.claude/settings.local.json

# Workflow generated artifacts (regenerable views / local telemetry — not source of truth)
backlog/board.md
backlog/index.html
backlog/.workflow/events.jsonl
backlog/.workflow/review-*.json
# Local-first sync state (regenerable; not source of truth)
backlog/.sync/

# Python (tools/, hooks, tests)
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.local-screenshots/
