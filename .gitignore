# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Dev EOD-import target DB (STORY-031) and local SQLite market-data builds
dev-market.db
*.db
# SQLite WAL/SHM sidecar files (regenerated alongside the DB)
*.db-shm
*.db-wal
# Pointer to the last-imported dev dataset (STORY-031) — local state, regenerable
.dev-active-db

# Claude Code personal settings
.claude/settings.local.json

# Workflow generated artifacts (regenerable views / local telemetry — not source of truth)
board.md
backlog/index.html
.workflow/events.jsonl

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
