{
  "type": "object",
  "properties": {
    "checks": {
      "type": "array",
      "minItems": 2,
      "maxItems": 2,
      "items": {
        "type": "object",
        "properties": {
          "id": { "enum": ["touch-scope", "diary"] },
          "verdict": { "enum": ["pass", "warn", "skip"] },
          "reason": { "type": "string", "maxLength": 200 },
          "evidence": {
            "type": "array",
            "maxItems": 5,
            "items": { "type": "string", "maxLength": 200 }
          }
        },
        "required": ["id", "verdict", "reason", "evidence"],
        "additionalProperties": false
      }
    }
  },
  "required": ["checks"],
  "additionalProperties": false
}
